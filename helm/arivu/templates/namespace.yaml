apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.instance.name }}
  labels:
    app: arivu
    instance: {{ .Values.instance.subdomain }}
    owner-email: {{ .Values.instance.ownerEmail }}

