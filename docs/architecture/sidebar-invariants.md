# Sidebar invariants (locked contract)

The sidebar is a **doctrine-enforced surface**. It renders **surfaces, identities, lenses, and governance — never raw entities**.

If a new sidebar item is needed, update:
- `SidebarStructure`
- `sidebar-invariants.md`
- `buildSidebarFromRegistry.ts`

